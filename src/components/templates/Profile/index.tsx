import React, { FunctionComponent } from "react";

interface TemplateProps {
	navbar?: React.ReactNode;
}

const ProfileTemplate: FunctionComponent = (props) => {
	return <div></div>;
};

export default ProfileTemplate;
